import { BaseTheme } from '@nextui-org/react/types/theme/types'

const sharedTheme: BaseTheme = {
  colors: {},
  space: {},
  fonts: {
    sans: "'Playfair Display', 'Raleway'",
  },
}

export default sharedTheme
